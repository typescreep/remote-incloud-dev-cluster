# ---------- Upstream (your port-forward target) ----------
UPSTREAM=http://127.0.0.1:9081

# ---------- Token source ----------
# Namespace & SA for token issuance (must exist in cluster, you created desktop-proxy before)
K8S_NAMESPACE=incloud-web
K8S_SERVICEACCOUNT=desktop-proxy

# Path prefixes that should receive Authorization if caller didn't set one
# AUTH_PATH_PREFIXES=/api/clusters/default/k8s
AUTH_PATH_PREFIXES=/

# Optional: set to "1" to always overwrite client Authorization with SA token
FORCE_AUTH=0

# Proxy listen port
PORT=8081

# Optional header overrides
# FORWARD_HOST=some-host.example
# INSECURE_SKIP_VERIFY=1   # only for self-signed upstream during local dev

DEBUG=1
